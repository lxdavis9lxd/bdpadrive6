<div class="explorer-container creation-page">
    <div class="explorer-header">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-md-8">
                    <h1 class="mb-2">
                        <i class="fas fa-link me-3"></i>
                        Create New Symlink
                    </h1>
                    <p class="mb-0 opacity-75">Create a symbolic link to another file or folder</p>
                </div>
                <div class="col-md-4 text-end">
                    <i class="fas fa-external-link-alt fs-1 opacity-25"></i>
                </div>
            </div>
        </div>
    </div>

    <div class="explorer-content">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-lg-8">
                    <div class="modern-file-card">
                        <div class="d-flex align-items-center mb-3">
                            <div class="file-icon symlink me-3">
                                <i class="fas fa-link"></i>
                            </div>
                            <h4 class="file-name mb-0">New Symlink Details</h4>
                        </div>

                        <% if (error) { %>
                        <div class="alert alert-danger border-0 bg-danger bg-opacity-10 text-danger rounded-3 mb-4">
                            <i class="fas fa-exclamation-triangle me-2"></i><%= error %>
                        </div>
                        <% } %>

                        <div class="alert alert-info border-0 bg-info bg-opacity-10 text-info rounded-3 mb-4">
                            <i class="fas fa-info-circle me-2"></i>
                            <strong>What is a symlink?</strong> A symbolic link (symlink) is a special file that points to another file or folder. 
                            When you click on a symlink, it will open the file or folder it points to.
                        </div>

                        <form action="/explorer/new-symlink" method="POST" class="modern-form">
                            <input type="hidden" name="path" value="<%= path %>">
                            
                            <div class="mb-4">
                                <label for="name" class="form-label fw-semibold text-dark">
                                    <i class="fas fa-signature me-2 text-primary"></i>
                                    Symlink Name <span class="text-danger">*</span>
                                </label>
                                <input type="text" class="form-control form-control-lg" id="name" name="name" required
                                       placeholder="Link to Important Document">
                                <div class="form-text">Enter the name for your new symlink</div>
                            </div>
                            
                            <div class="mb-4">
                                <label for="targetId" class="form-label fw-semibold text-dark">
                                    <i class="fas fa-bullseye me-2 text-warning"></i>
                                    Target File/Folder <span class="text-danger">*</span>
                                </label>
                                <select class="form-select form-select-lg" id="targetId" name="targetId" required>
                                    <option value="">Select a file or folder to link to...</option>
                                    <% nodes.forEach(node => { %>
                                        <% if (node.type !== 'symlink') { %>
                                        <option value="<%= node.node_id %>">
                                            <% if (node.type === 'file') { %>
                                                📄 <%= node.name %>
                                            <% } else { %>
                                                📁 <%= node.name %>
                                            <% } %>
                                        </option>
                                        <% } %>
                                    <% }) %>
                                </select>
                                <div class="form-text">
                                    Choose the file or folder that this symlink should point to. 
                                    Note: You can only link to files and folders you own.
                                </div>
                            </div>
                            
                            <% if (nodes.filter(n => n.type !== 'symlink').length === 0) { %>
                            <div class="alert alert-warning border-0 bg-warning bg-opacity-10 text-warning rounded-3 mb-4">
                                <i class="fas fa-exclamation-triangle me-2"></i>
                                <strong>No files or folders available</strong><br>
                                You need to create some files or folders first before you can create symlinks to them.
                            </div>
                            <% } %>
                            
                            <div class="file-actions d-flex gap-3 justify-content-center">
                                <button type="submit" class="btn btn-primary btn-lg px-4" 
                                        <%= nodes.filter(n => n.type !== 'symlink').length === 0 ? 'disabled' : '' %>>
                                    <i class="fas fa-save me-2"></i>Create Symlink
                                </button>
                                <a href="/explorer?path=<%= encodeURIComponent(path) %>" class="btn btn-outline-secondary btn-lg px-4">
                                    <i class="fas fa-arrow-left me-2"></i>Back to Explorer
                                </a>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
